import React from 'react';
import Card from '../Reusable/Card';

function CardWithProfileImage(Component) {
  const wrappedComponent = ({ title, description }) => (
    <h1>hello world</h1>
  );

  return wrappedComponent;
}

export default CardWithProfileImage;
